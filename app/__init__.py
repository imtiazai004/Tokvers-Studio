"""New SaaS API (FastAPI) built on the multi-tenant `core` layer.

Additive: this runs alongside the legacy `main.py` until the new app is proven,
then the entrypoint is switched over. Nothing here touches the old code paths.
"""
